<% if !@new_ags.empty? %>
  <% @new_ags.each do |ag| %>
    $('#tinderslide > ul').prepend("<%= j render partial: 'activities/activity', locals: {activity: ag.activity} %>");
  <% end %>
<% end %>

setTimeout(function(){
  jTinderSwipeEffect();
}, 500);
