
<div class="container">


    <% if !@open_activities.empty? %>
    <h2>Open Activities</h2>
    <div class="open-activities">
        <%= render @open_activities %>
    </div>
    <% end %>
  <br><br>
    <% if !@closed_activities.empty? %>
    <h2>Closed Activities</h2>
    <div class="closed-activities">
        <%= render @closed_activities %>
    </div>
    <% end %>
</div>
