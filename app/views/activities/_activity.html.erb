
<div class="col-md-4 col-xs-12" style="border-style:solid;border-color:grey;">
  <div class="interest">
      <div class="dislike">
        I don't want to go  <span class="glyphicon glyphicon-thumbs-down" ></span>
      </div>
      <div class="like">
        I want to go  <span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
      </div>
  </div>
  <div class="">
    <%= image_tag(activity.image_url, class: "img-responsive") %>
  </div>
  <div class="">
    <%= activity.name %>
  </div>
  <div class="">
    <%= link_to "edit this", edit_activity_path(activity) if (logged_in? && current_user.id == activity.host_id) %>
  </div>
  <div class="">
    <%= link_to "delete this", activity_path(activity), method: :delete if (logged_in? && current_user.id == activity.host_id) %>
  </div>
  <div>
      Tags:
    <% activity.tags.each do |tag| %>
      <a href="#"><%= " " + tag.name + " " %></a>
    <% end %>
  </div>
  <div class="user" style="border-style:solid;border-color:grey;">
    <%= image_tag(activity.host.image_url, class: "col-md-3 col-xs-3") %>
    <%= "Host: " + activity.host.name %>
  </div>
</div>
